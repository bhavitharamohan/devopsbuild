groups:
  - name: app-alerts
    rules:
      - alert: ApplicationDown
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Application Down"
          description: "Application not reachable for more than 1 minute."
